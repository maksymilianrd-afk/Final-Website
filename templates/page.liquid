<section class="scene scene-surface" style="padding-top:8rem">
  <div class="scene__inner" style="max-width:680px">
    <h1 class="h2">{{ page.title }}</h1>
    <div class="lead" style="margin-top:2rem">{{ page.content }}</div>
  </div>
</section>
